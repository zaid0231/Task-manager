{
  "name": "ethara-project-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Project and task tracker with role-based access",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "node -e \"require('./db').ensureSchema().then(() => console.log('Migrations complete')).catch((err) => { console.error(err); process.exit(1); })\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "knex": "^2.5.1",
    "pg": "^8.11.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
